
// let massMark = 78;
// let heightMark = 1.69;

// let massJohn = 92;
// let heightJohn = 1.95;

// let bmiMark = massMark / heightMark ** 2;
// let bmiJohn = massJohn / heightJohn ** 2;

// let markHigherBMI = bmiMark > bmiJohn

// let mainMessage = `Marks BMI is ${bmiMark}, and Johns BMI is: ${bmiJohn}`;
// let bonusMessage = (markHigherBMI) ? `Marks BMI ${bmiMark} is higher than Johns ${bmiJohn}` : `Johns BMI ${bmiJohn} is higher than Marks ${bmiMark}`;

// console.log(mainMessage);
// console.log(bonusMessage);

// massMark = 95;
// heightMark = 1.88;

// massJohn = 85;
// heightJohn = 1.76;

// bmiMark = massMark / heightMark ** 2;
// bmiJohn = massJohn / heightJohn ** 2;

// markHigherBMI = bmiMark > bmiJohn
// mainMessage = `Marks BMI is ${bmiMark}, and Johns BMI is: ${bmiJohn}`;
// bonusMessage = (markHigherBMI) ? `Marks BMI ${bmiMark} is higher than Johns ${bmiJohn}` : `Johns BMI ${bmiJohn} is higher than Marks ${bmiMark}`;

// console.log(mainMessage);
// console.log(bonusMessage);